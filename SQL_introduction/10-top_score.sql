-- Task: List all records from second_table showing score and name, ordered by score descending
-- This query selects score and name from second_table and orders them by score (top first)
SELECT score, name FROM second_table
ORDER BY score DESC;
